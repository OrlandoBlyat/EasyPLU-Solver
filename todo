FRONTEND
    Fix front end loading bar to show realtime info,
    Show API status,
    Fix issue with full database missing when user that initialises it, has no/low amount of PLUs to learn,
    Do SSO login option if god so provides,
    Add show password button,
    Language menu,
    Maybe... custom logo?.

BACKEND
    Fix issue with full database missing when user that initialises it, has no/low amount of PLUs to learn,
    Do SSO login option if god so provides,
    Loop until 100% option,
    Add AiO starting script, front/back end.

DOCS
    Add how to run the API